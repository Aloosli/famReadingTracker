# One dependency (@zxing/library) declares an overly-strict Node >=24 requirement, but the app
# runs fine on Node 20+. Strict engine checking would block `npm install` on those versions for
# no real reason, so it's disabled here to keep first-time setup smooth.
engine-strict=false
